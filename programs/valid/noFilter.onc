script noFilter(a a, b b)
/** test */
use a.grp, b.grp
use t.grp

group Id x = {1, 2, <x>, 1200 to 123123}
group Id x = {1, 2, <x>, 1200 to 123123}	

{
	foreach Patient p 
	{
		print p
	}
	foreach Patient p 
	{
		print p
		foreach Doctor D
		{
		}
		foreach Doctor D
		{
			foreach Doctor D
			{
			}
		}
		
	}

	table t = count Patients by Birthyear
	print t
}
