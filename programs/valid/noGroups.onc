script noGroups(a a, b b)
/** test */
use a.grp, b.grp
use t.grp

{
	foreach Patient p 
	{
		print p
	}
	foreach Patient p 
	{
		print p
		foreach Doctor D
		{
		print oncologist of D
		}
		foreach Doctor D
		{
			foreach Diagnosis D
			{
			}
		}
		
	}

	table t = count Patients by Birthyear
	print t
}
