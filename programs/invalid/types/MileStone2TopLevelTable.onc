script MileStone2TopLevelTable()
	/*table and list definitions have to be in top level*/

{
	list a = sequences like [patient_arrived -> patient_arrives]
	foreach member s in a{
		table b = count Patient by id
	}


}
